/**
 * Exam picker UI is paused — keep file for later multi-exam launch.
 * Always returns null so guests/users aren't asked to pick SSC / Banking / Railways.
 */
export function ExamPicker(_props: { force?: boolean } = {}) {
  return null;
}
