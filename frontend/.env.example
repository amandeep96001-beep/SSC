# Copy to .env.local for Vite:
#   cp .env.example .env.local

# Same Web Client ID as backend GOOGLE_CLIENT_ID
# Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID (Web)
VITE_GOOGLE_CLIENT_ID=
